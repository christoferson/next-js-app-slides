/**
 * Tailwind v4 — the whole config. v4 moved theme configuration into CSS (`@theme` in
 * `app/globals.css`), so there is no `tailwind.config.js` to keep in sync with it.
 */
const config = {
  plugins: { "@tailwindcss/postcss": {} },
};

export default config;
